// services/spotifyService.js
const axios = require('axios');

const SpotifyService = {
    getAuthorizationUrl: () => {
        // Implement logic to create and return the authorization URL
    },

    saveDiscoverWeekly: async (code) => {

    }
};

module.exports = SpotifyService;
